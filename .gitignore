# Dependencies
node_modules/

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# VS Code
.vscode/*
!.vscode/settings.json   # keep shared settings, ignore everything else
*.code-workspace

# Root lockfile only (do NOT block /web/pnpm-lock.yaml)
#/pnpm-lock.yaml       <-- uncomment if a root lockfile appears; keep the leading slash
/pnpm-lock.yaml

# Env at root (safe even if you don’t have any here)
.env
.env.*

# Supabase local state
.supabase/
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# Add to .gitignore
!.claude/
.claude/settings.local.json  # Keep project context, ignore local settings

# Session context and documentation (local only)
handoffs/
docs/
features/
nextsession/
prompts/
build-plan/
revise.md