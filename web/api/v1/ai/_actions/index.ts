// Purpose: Export all AI gateway actions for router
// Actions exported here are automatically routable via ?action=<name>

export { generateQuiz as generate_quiz } from './generate.js';
export { grade } from './grade.js';
export { health } from './health.js';
